-- RAFD | رفد - Seed data for local development
-- This file is applied after migrations on `supabase start` and `supabase db reset`
-- Keep only dev/test data, not production secrets

-- Example: Insert demo tenant if not exists (optional, commented)
-- INSERT INTO tenants (id, name, name_ar, currency, plan, status) 
-- VALUES (1, 'Demo Store', 'متجر تجريبي', 'YER', 'growth', 'active')
-- ON CONFLICT (id) DO NOTHING;

-- Platform settings already seeded in base migration
-- Subscription plans already seeded in base migration

-- Add any additional dev seed below:
